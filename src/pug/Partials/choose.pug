section.choose
    .container
        .choose__title
            +title(title.choose.sub,title.choose.big)
        .row
            each val in choose__item
                +choose__item(val.icon, val.title, val.desc, val.link)