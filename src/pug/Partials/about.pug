section.about#about
    .container
        .row
            .about__item
                +title(title.about.sub_title,title.about.big_title)
                a(href="#")= about_link_content
                    i.fa.fa-arrow-right
            .about__item
                .row
                    each val in about_img
                        .about__img
                            img(src= val.src, alt= val.alt)